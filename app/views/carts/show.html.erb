<p id="notice"><%= notice %></p>

<h2>Programaticカート</h2>
<% @cart.line_items.each do |item| %>
  <li><%= item.quantity %> &times; <%= item.product.title %></li>
<% end  %>
<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
